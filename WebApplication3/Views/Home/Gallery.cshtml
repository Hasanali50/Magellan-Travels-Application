@using FluentBootstrap
@model IEnumerable<WebApplication3.Models.Gallery>
@{
    ViewBag.Title = "Gallery";
}

<h2>Index</h2>


    @foreach (var pic in Model)
    {   @pic.PicturePath;
        @pic.PictureDesc;
    }

